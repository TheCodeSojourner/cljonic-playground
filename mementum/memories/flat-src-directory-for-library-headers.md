💡 For this repository, development headers should stay in a flat `src/` layout (for example `src/vector.hpp`, `src/map.hpp`, `src/set.hpp`, `src/concepts.hpp`) rather than nested library subdirectories. Even though release packaging uses a single amalgamated header, a flat source tree is currently preferred to keep file discovery and include paths simple during early library evolution.
