💡 For collection API design in cljonic, free operations should prioritize Clojure-like semantics and naming whenever embedded constraints permit. Treat this as the default direction for standalone operations (`count`, `get`, `assoc`, `dissoc`, `conj`, `contains`, `first`, `rest`): preserve Clojure behavior shape first, then document any constrained divergence explicitly in specs and tests.
